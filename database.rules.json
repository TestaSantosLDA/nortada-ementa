{
  "rules": {
    ".read": false,
    ".write": false,
    "ementas": {
      "$periodo": {
        ".read": "auth != null",
        ".write": "auth != null",
        "$dia": {
          ".validate": "newData.isString() && newData.val().length <= 500"
        }
      }
    }
  }
}
