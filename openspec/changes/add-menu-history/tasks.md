## 1. Regras

- [x] 1.1 Mover `.read` para o topo dos nós `ementas` e `reviews` em `database.rules.json` e publicar.

## 2. Índice de pratos

- [x] 2.1 Listeners na raiz de `ementas` e `reviews`; índice lazy normalizado (display mais recente, última data, média de estrelas), invalidado quando chegam dados.

## 3. Autocomplete

- [x] 3.1 Chips sob o textarea da ementa, visíveis só no painel de detalhe, filtrados enquanto se escreve; toque preenche e guarda.

## 4. Roleta

- [x] 4.1 Botão em dias sem ementa (zona de sugestões do painel): exclui últimos 14 dias, ordena por estrelas + antiguidade, escolhe aleatoriamente entre os 5 melhores, preenche a ementa.

## 5. Estilos e verificação

- [x] 5.1 Estilos dos chips e do botão.
- [x] 5.2 Sintaxe verificada, app carrega localmente sem erros; commit e push.
